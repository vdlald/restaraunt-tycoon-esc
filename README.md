# RestaurantTycoonEcs
Generated by [Rojo](https://github.com/rojo-rbx/rojo) 7.7.0.

## Getting Started
To build the place from scratch, use:

```bash
rojo build -o "RestaurantTycoonEcs.rbxlx"
```

Next, open `RestaurantTycoonEcs.rbxlx` in Roblox Studio and start the Rojo server:

```bash
rojo serve
```

For more help, check out [the Rojo documentation](https://rojo.space/docs).

## Важное

В новых версиях ProfileStore используется :StartSessionAsync() вместо старого :LoadProfileAsync(), а завершение сессии — :EndSession() вместо :Release() (ProfileStore access and change data). Не забудьте включить Studio → Game Settings → Security → Enable Studio Access to API Services, иначе DataStore не работает в Studio